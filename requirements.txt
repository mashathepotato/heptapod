# HEPTAPOD Requirements
# Install with: pip install -r requirements.txt

# Core Dependencies
# ================

# Orchestral AI Framework (LLM orchestration)
# 1.5+ ships a lean dependency tree; install whichever LLM SDK you actually
# use (openai is pinned below; add anthropic / groq / mistralai / google-genai
# yourself if you switch providers).
orchestral-ai>=1.5.0

# LLM Provider SDKs
openai>=2.13.0           # GPT API (used via orchestral.llm.GPT)
ollama>=0.6.1            # Local models (used via orchestral.llm.Ollama)

# Scientific Computing
numpy>=2.3.5

# Physics-Specific Packages
# ==========================
pythia8mc>=8.316.0       # Pythia8 Python bindings (requires Python 3.12 or 3.13, NOT 3.14+)
sherpa-mc>=3.0.3b1       # Sherpa event generator (requires Python 3.12 or higher)
pylhe>=1.0.2             # LHE file parsing
particle>=0.26.0         # Particle data and PDG IDs
pdg>=0.2.0               # PDG database API (particle properties, branching fractions)
feyngraph                # Feynman diagram enumeration

# Utilities
python-dotenv>=1.2.1     # Environment variable management
requests>=2.32.0         # HTTP requests (used by tools/inspire/inspire_interface)
sympy>=1.14.0            # UFO conversion
tqdm>=4.67.0             # Progress bars (used by tools/analysis/conversions, pythia, sherpa)

# Development Tools (Optional)
# =============================
# Uncomment these for development:
# pytest>=7.4.0          # Testing framework
# black>=23.0.0          # Code formatting
# flake8>=6.0.0          # Linting
# mypy>=1.0.0            # Type checking
